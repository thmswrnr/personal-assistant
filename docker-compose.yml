services:
  core:
    build:
      context: ./core
      dockerfile: Dockerfile
    # Named explicitly, and shared with core_bridge below — the two run the same image, and
    # without a fixed name compose derives one per service and stores the same 900MB twice.
    image: core-harness
    container_name: core_harness
    # Run as the unprivileged "node" user (uid 1000) so files the agent creates in the
    # mounted volumes are owned by the host user, not root.
    user: "node"
    volumes:
      - ./data/pi:/app/.pi:rw
      - ./data/storage:/app/storage:rw
      - ./data/secrets:/app/secrets:rw
      # The skills already arrive with the .pi mount above. Re-mount them read-only so the
      # agent cannot edit its own tracked skill packages (it writes to storage/custom_skills).
      - ./data/pi/skills:/app/.pi/skills:ro
    env_file:
      - .env
    # Reach services running on the HOST — Home Assistant on :8123 (the `home-assistant` skill),
    # or a self-hosted model server if you run one. Linux doesn't provide host.docker.internal by
    # default; host-gateway maps it to the bridge gateway.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      # Use the mounted .pi dir as pi's config dir (models.json, SYSTEM.md, skills/).
      # The model is an external OpenAI-compatible API configured in data/pi/models.json +
      # data/pi/settings.json (key via LLM_API_KEY in .env) — no local model service required.
      - PI_CODING_AGENT_DIR=/app/.pi
      # Local timezone, so dates and times Core reports are local. Change as needed.
      - TZ=Europe/Berlin
    # Core does nothing on its own — it acts only when asked. So the main process is just a
    # keep-alive: it holds the container open as a warm shell that `core.sh` (and any future
    # service on this box) enters with `docker exec pi …`.
    command: ["sleep", "infinity"]
    # Come back after a reboot or a crash — Core is meant to be reachable 24/7. "unless-stopped"
    # (not "always") so a deliberate `docker compose stop` stays stopped.
    restart: unless-stopped
    networks:
      - core_network

  searxng:
    # Self-hosted metasearch for the websearch skill. Internal-only (no host port) —
    # reachable by core/ui at http://searxng:8080. Pinned by digest for reproducibility.
    # No own index: it aggregates upstream engines and exposes a private JSON API.
    image: searxng/searxng@sha256:8f1e32b1013b1e74a9b769686edbca03ff5439395743725f3f18369b4841f709
    container_name: core_searxng
    volumes:
      # settings.yml enables the JSON API; the secret comes from SEARXNG_SECRET (.env).
      # Mount the FILE, read-only — not the directory. The entrypoint chowns any writable
      # config dir to its own uid, which then locks the host user out of their own tracked
      # file (a plain `git checkout` of it fails). settings.yml is the only thing SearXNG
      # writes here, and it doesn't need to.
      - ./data/searxng/settings.yml:/etc/searxng/settings.yml:ro
    environment:
      - SEARXNG_SECRET=${SEARXNG_SECRET:?set SEARXNG_SECRET in .env (openssl rand -hex 32)}
      - SEARXNG_BASE_URL=http://searxng:8080/
    restart: unless-stopped
    networks:
      - core_network

  core_bridge:
    # Core, wearing an Ollama server's clothes, so Home Assistant's built-in Ollama conversation
    # agent can talk to it. Same image as core — it spawns its own `pi --mode rpc` process.
    build:
      context: ./core
      dockerfile: Dockerfile
    # Same image as core, by name. The build is a cache hit, so this costs nothing and keeps
    # one copy on disk instead of two.
    image: core-harness
    container_name: core_bridge
    user: "node"
    volumes:
      - ./data/pi:/app/.pi:rw
      - ./data/storage:/app/storage:rw
      - ./data/secrets:/app/secrets:rw
      - ./data/pi/skills:/app/.pi/skills:ro
      # The bridge's own source. Read-only: the agent it runs must not rewrite its own server.
      - ./bridge:/app/bridge:ro
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - PI_CODING_AGENT_DIR=/app/.pi
      - TZ=Europe/Berlin
      # The voice model, pinned SEPARATELY from data/pi/settings.json — that file is tuned for the
      # terminal, where waiting is fine. Home Assistant abandons a slow turn, and a reasoning model
      # took 88 seconds to answer "hallo". So voice gets a non-reasoning model with thinking off.
      # Changing the CLI default must never break voice again.
      - BRIDGE_MODEL=${BRIDGE_MODEL:-comma-soft/gemma4-31b-instant}
      - BRIDGE_THINKING=${BRIDGE_THINKING:-off}
    ports:
      # Loopback ONLY. This is an unauthenticated endpoint that runs Core's tools — anyone who
      # can reach it can drive the agent. Home Assistant runs with host networking, so it finds
      # it at 127.0.0.1:11434 on the same box.
      - "127.0.0.1:11434:11434"
    command: ["node", "/app/bridge/server.mjs"]
    restart: unless-stopped
    networks:
      - core_network

  core_webui:
    # TRIAL BRANCH: @agegr/pi-web. A browser chat window for Core — a session sidebar you can
    # resume from the phone, a streaming thinking output, and a file explorer for the things
    # Core downloads and writes. Judge it against the criteria in the plan, then keep or drop.
    #
    # Same image as core and core_bridge. That is the point: this UI runs the agent IN-PROCESS,
    # so it needs the same git/jq/gh/yt-dlp that Core's skills shell out to, plus the same
    # mounts. Anything less would be a different agent wearing Core's name.
    build:
      context: ./core
      dockerfile: Dockerfile
    image: core-harness
    container_name: core_webui
    profiles: ["webui"]
    user: "node"
    volumes:
      - ./data/pi:/app/.pi:rw
      - ./data/storage:/app/storage:rw
      - ./data/secrets:/app/secrets:rw
      - ./data/pi/skills:/app/.pi/skills:ro
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # /app is the workspace: this UI has no --cwd flag, so it takes the working directory.
    working_dir: /app
    environment:
      - PI_CODING_AGENT_DIR=/app/.pi
      - TZ=Europe/Berlin
      # HTTP Basic Auth (user "pi"). REQUIRED, not optional: without it the package prints a
      # warning and serves an agent that runs arbitrary tools to anyone on the network. Compose
      # refuses to start rather than expose that, so set WEBUI_PASSWORD in .env.
      - PI_WEB_PASSWORD=${WEBUI_PASSWORD:?set WEBUI_PASSWORD in .env before starting core_webui}
      # Next.js rejects a Host header it does not know. Add the Pi's hostname and LAN IP here,
      # comma-separated, or the phone gets a blank page.
      - PI_WEB_ALLOWED_HOSTS=${WEBUI_ALLOWED_HOSTS:-}
    ports:
      # LAN, not loopback — the whole point is reaching it from the couch. Basic Auth above is
      # the only thing in front of it, and it travels in clear over HTTP, so this belongs on the
      # home network ONLY. Do not port-forward it.
      - "0.0.0.0:30141:30141"
    # -H 0.0.0.0 so it answers inside the container's network namespace, not just its loopback.
    # --no-open because there is no browser in here to open.
    command: ["pi-web", "-H", "0.0.0.0", "-p", "30141", "--no-open"]
    restart: unless-stopped
    networks:
      - core_network

  homeassistant:
    # Home automation. Only starts under the "home" profile, so a dev box without the Zigbee
    # stick still runs `docker compose up -d` unchanged. On the Pi:
    #   docker compose --profile home up -d
    profiles: ["home"]
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: core_homeassistant
    # Host networking, so HA can discover devices on the LAN (mDNS/SSDP need broadcast traffic,
    # which a bridge network drops). Side effect: the UI is on the host's :8123 directly, and HA
    # reaches core_bridge at 127.0.0.1:11434 rather than by container name.
    network_mode: host
    # HA manages the radio and mounts/unmounts USB devices; it expects to run privileged.
    privileged: true
    volumes:
      - ./data/homeassistant:/config
    devices:
      # The Zigbee stick. Set ZIGBEE_DEVICE in .env to the stable by-id path — NOT /dev/ttyUSB0,
      # which is assigned in plug order and moves between boots. Find it with:
      #   ls -l /dev/serial/by-id/
      #
      # Unset it and this maps /dev/null, so Home Assistant still starts — everything except the
      # radio works, which is what you want before the stick arrives. ZHA will simply find
      # nothing on /dev/ttyUSB0 until you point this at the real device and recreate the
      # container. (Compose interpolates every service before it filters profiles, so a `:?`
      # here would break `docker compose up` even on a machine that never starts this service.)
      - ${ZIGBEE_DEVICE:-/dev/null}:/dev/ttyUSB0
    environment:
      # Same local timezone as core, so automations and Core agree on what "today" means.
      - TZ=Europe/Berlin
    restart: unless-stopped

  whisper:
    # Speech to text for Home Assistant's Assist pipeline. The phone does NOT transcribe — the
    # companion app only runs the wake word on-device (microWakeWord) and streams the audio to
    # Home Assistant, which needs an STT engine of its own. This is it.
    #
    # Same "home" profile as homeassistant: Pi only.
    profiles: ["home"]
    image: rhasspy/wyoming-whisper
    container_name: core_whisper
    # Host networking, so HA's Wyoming integration finds it at 127.0.0.1:10300 like any other
    # satellite service on this box.
    network_mode: host
    volumes:
      - ./data/whisper:/data
    # base-int8, not tiny: tiny mangles anything longer than a short sentence, and murders proper
    # nouns ("Flohmarkt in der Rheinaue" came back as "Floom macht in der Rheinau"). base is
    # roughly 1.5s for a 3-second clip on a Pi 4 versus under 1s for tiny — worth it. If it still
    # misreads you, small-int8 is the next step up, but expect several seconds on a Pi 4.
    #
    # --language de: pinned, not auto-detected. Pinning is more accurate than letting whisper
    # guess, and this assistant is spoken to in German. For a second language, run another
    # container on a different port with its own --language and give it its own Assist pipeline.
    command: ["--model", "base-int8", "--language", "de", "--uri", "tcp://0.0.0.0:10300", "--data-dir", "/data"]
    restart: unless-stopped

networks:
  core_network:
    driver: bridge
