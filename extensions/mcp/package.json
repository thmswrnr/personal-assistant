{
  "name": "pi-mcp-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bridge MCP server tools into pi as native tools (config-driven via mcp.json)",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
