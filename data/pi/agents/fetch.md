---
description: 'Fast worker for simple, single-step fetches: run ONE skill/CLI command (or a couple of independent ones) and return its raw output verbatim — e.g. fetch unread email, today''s calendar, current weather, a feed. Use this for quick data-gathering with no reasoning, especially when fanning out several such fetches in parallel. Do NOT use it for multi-step reasoning or open-ended analysis (use general-purpose), code search (use Explore), or planning (use Plan).'
tools: read, bash
model: alan/comma-soft/gemma4-31b-instant
prompt_mode: append
---
